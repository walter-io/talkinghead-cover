---
name: workbuddy-talkinghead-cover
description: 生成口播赛博朋克封面 / WorkBuddy 品牌风格的 Q 版卡通口播封面（青绿 #28b894 + 紫色 #6c4dff 霓虹放射背景、居中 Q 版人物、顶部大标题、四周文字标签牌）。**本技能默认必须调用「Agnes图像生成」技能（agnes-image-2.5-flash）生成底图**，无需用户提醒。当用户提供「人物形象 + 标题 + 标签」并要求出封面图时使用；支持自媒体口播视频/图文封面、多标签功能展示封面。触发词：口播赛博朋克封面、赛博朋克口播封面、口播封面、赛博朋克封面、霓虹封面、封面、WorkBuddy 封面、生成封面图、做封面。
agent_created: true
---

# WorkBuddy 口播封面生成

用户在自媒体运营中会反复制作「同一风格、换标题/换标签」的口播封面。本技能把已定稿的视觉规范固化下来：用户只需提供 **人物形象图 + 标题文案 + 标签文案**，即可产出统一风格的封面。

## ⚠️ 第一原则：底图一律用 Agnes 生成（无需用户提醒）

**默认动作，不必等人说「用 Agnes」**：

1. 底图（人物 + 背景 + 科技感元素）**必须调用「Agnes图像生成」技能**（`agnes-image-2.5-flash`）生成，不要用 PIL 拼、不要找现成图、不要跳过。
2. 调用方式：用 `Skill` 工具加载 `Agnes图像生成`，按其 `scripts/agnes_image.cjs` 走 **图生图**（`--image <人物形象图>`）。
3. 本技能自带的 `scripts/agnes_img2img.cjs` 是等价封装（配置驱动），**二者择一即可**；若走本技能脚本，就把「Agnes图像生成」技能一并加载以遵守其接口约定。
4. **只有两种情况可以跳过 Agnes**：① 用户明确说「不要动底图 / 只改文字」；② 复用自己的历史底图（如 `wb-cover-tech-base2.png`）只重排文字。除此以外，**必须先跑 Agnes**。
5. 文字（标题 + 标签）**永远不交给 Agnes 渲染**，一律用 PIL 精确叠加——这条不受上面的例外影响。

> 典型触发场景：用户只发「人物形象 + 标题 + 标签」三样东西，你就应当自动完成「Agnes 出底图 → PIL 叠文字 → 像素校验 → present_files」全流程，中途不反问「要不要用 Agnes」。

## 视觉规范（已与用户确认，不要随意改动）

- 画布：竖版 **9:16**，输出 **1472×2624**（Agnes 2K 9:16）。
- 品牌色：青绿 `#28b894`、紫色 `#6c4dff`；背景为青绿→紫的放射状渐变光效，点缀紫色五角星。
- 人物：**Q 版全身小人**（头大身小、萌系比例的可爱画风），居中，**画面上方约 2/5 留空**给标题。
  - 全身版推荐 prompt 关键词：「可爱的 Q 版全身小人，头部偏大、身体矮胖、手脚短圆的萌系比例」「完整全身（头发顶端到鞋子）可见，不被裁切」「一只手举起做讲解手势（食指向上）」。
  - 想要**俯视角度**（更可爱）时加：「采用俯视视角（从上往下看的角度）来画，因此头显得很大、身体显得短小」。
  - 人物纵向区间：头顶约 39%~43%，脚底约 97%~98%。
- 标题：两行（中文+中文，或英文+中文）；**白色填充 + 黑色描边**，中文用 **微软雅黑粗体 `msyhbd.ttc`**（正规清晰，用户要求「文字用正规点」），英文/数字可由雅黑自带字形覆盖；字号约 165~215，整体 **6° 左低右高倾斜**，贴顶部。
  - **重点词用 `{}` 包住**：会自动放大 `title_emph_ratio` 倍（默认 1.28）并换成**鲜艳色 `title_emph_color`（默认亮黄 `#FFD400`）**，形成「白字打底 + 黄字强调」的层级。
- 标签：圆角胶囊牌（青绿→紫对角渐变底 + 白色外描边），白色粗体中文，分左右两列排在人物两侧。
- 参考成品：`C:\Users\we\agnes-output\cover-wb-token-v9.png`（半身大头 + 环绕科技面板，最新基准）、`cover-wb-egg-token-v3.png`（全身 Q 版 + 6 标签）、`cover-wb-autocheckin-v3.png`（半身 + 4 标签）。

## 关键经验（务必遵守）
1. **绝不要用 Agnes 渲染中文/文字。** 实测 Agnes 会把「多账号管理」写成「多帐号管理」、「token统计」错成「toten定计」。所有文字（标题 + 标签）一律用 PIL + 字体精确叠加。
2. **中文长标题要有字体兜底。** 默认字体（微软雅黑粗体）字库很全，一般不会缺字；但若切到站酷快乐体等字库较小的字体，渲染较长中文行时**偶尔出现豆腐块**（实测「我们这代的鸡蛋」首次渲染成方框，重跑即恢复）。`compose_cover.py` 已内置检测：若当前字体缺字形，自动改用兜底字体（`msyhbd.ttc` 等）；缺字形检测依赖 `fontTools`（需 `pip install fonttools`）。注意：**出现方块时先重跑一次**，多为渲染偶发问题而非真缺字。
3. **标题两行可以是「中文行 + 中文行」**，不必强制英文+中文。`title.line1` / `title.line2` 都走中文渲染路径（英文/数字仍由该字体自带字形覆盖）。
4. **Agnes 只负责生成底图**（人物 + 背景 + 星星，**无任何文字、无图标面板**）。底图 prompt 里要显式声明「画面里不要出现任何文字、字母、数字」。
5. **Agnes 图生图会让画面元素整体位移**（实测人物会被上移/重排 4~9%）。因此凡「某元素必须纹丝不动」的需求都不能用全图 Agnes 重绘，只能做像素级局部编辑。
6. 顶部 1/3（或 2/5）一定要留空，否则标题会压住顶部元素；生成后要用像素验证而非目测。
7. 标签居中定位时注意不要超出画布：左标签 `fx` 建议 ≥ 0.21（宽 ~520px 时），右标签 `fx` ≤ 0.80。
8. **标签多（5~6 个）时**：左右两列、从上到下交替排布，`fy` 从 0.545 铺到 0.815，字号降到 66~86，避免遮挡人物。
9. **标题与人物的间距要「贴近但不相交」，用数字量化。** 把标题最后一行文字的 y 底与人物头顶 y 相比，**目标间隙 2.5%~6% 画面高度**（用户偏好紧凑，实测 2.7% 效果好）；间隙过大（>7%）就把 `title_top` 往下加。注意：标题字号变大后，若仍从顶部贴起会出现大空隙，需同步把 `title_top` 调大（实测把字号从 165/185 提到 195/215 后，`title_top` 需从 0.032 提到 0.088；再逼近到 2.7% 间隙则为 0.116）。
10. **标题强调词用 `{}` 标记放大。** 想突出某个词（如「鸡蛋」「免费」），在 `title.line1/line2` 里写 `我们这代的{鸡蛋}`，脚本会把 `{}` 内的字放大 `title_emph_ratio` 倍（默认 1.28），并**与原文字基线对齐**、同行混排。放大倍数建议 1.25~1.35，过大行高会被撑开。
11. **标签一律配彩色小图标**，纯文字太单调。`compose_cover.py` 内置 10 种图标（gift/coin/star/bolt/paw/rocket/code/phone/heart/sparkle），默认按标签文字自动匹配（如「送token」「送积分」→ gift，「小米mimo」「catpaw」→ paw，「zocde」→ code）；也可在 `labels[].icon` 显式指定。加图标后标签会变宽约 1.3 倍，**左侧标签 `fx` 要相应加大到 0.245 左右**（原 0.205 会贴出画布）。
12. **科技感装饰图标（`decor` 段）默认开启**，用来给画面补科技氛围。线框霓虹风（不画实心底盘）、半透明 alpha 130~170、按青绿/紫/电光青三色循环。可用图标：`chip`(芯片) `bolt`(闪电) `signal`(信号) `hex`(六边形) `gear`(齿轮) `spark`(四芒星) `atom`(原子) `globe`(网格球) `code`(代码)。
13. **装饰图标绝不与标题重合——靠数据定位，不靠目测。** 顺序：先渲染标题 → `add_title()` 返回标题外接矩形 → `add_decor()` 用该矩形做相交判定（`avoid_title=true`），相交的图标自动下推到标题下方；推不动就跳过并打印 `skipped`。
    - **踩过的坑**：装饰项 `fy` 若落在标题的纵向区间内（本模板标题实际占 **13.5%~39.3%**，斜排后横向铺满 3.9%~94.4%），会**全部被推挤到同一个 y**、叠成一排。所以 `fy` 必须避开标题带。
    - **本模板实测的安全区**：`fy` 取 0.04（顶缘）、0.45（标题下沿与人物头顶之间的空白带）、0.61 / 0.79（人物两侧）、0.945（底部）；`fx` 取 0.05~0.075 / 0.93~0.95（贴左右边缘的窄条）。
    - 交付前必须跑一次像素校验（复用 `_verify_v5.py` 思路）：把新旧成品做差得到「新增像素=装饰」，再与旧元素掩膜求交，**重合像素必须为 0**。
14. **标题支持任意行数，且自动保证「文字绝不超出画布」。** 用 `title.lines` 数组可写 2 行以上；`add_title()` 内置两级防溢出：
    - 先按 `title_safe_margin`（默认 0.035）算出 `max_w`，并在计算时**额外扣除旋转 6° 带来的外接宽度增量**（`H*0.42*sin(angle)*0.5`），超宽则自动整行缩字号重排（最多迭代 24 次）。
    - 旋转后若仍超宽，**整体等比缩放而非裁切**（宁可字小，不可切字）。落位后应满足「左右各留 ≥ 5px」。
    - 实测：240 号字的 `WorkBuddy` 原宽 1670 > 上限 1254 → 自动缩到 176 号，最终落位 x 7-1465，两端不切。
15. **参考图（爆款封面）的真实排版规律**（本地样本 `C:\Users\we\agnes-output\ref_A~D.png`）：
    - **标题从 y≈5% 贴顶起排**，密集排到 **y≈38~40%**，横向 x 7%~94% **逐行顶满**（不是居中留白）。
    - 白字 + **粗黑描边**（约 16-17px），**6° 倾斜**，字号极大。
    - **完全不使用胶囊标签牌**——信息全交给标题承载。
    - **人物 + 科技元素占 y 40%~100%**：核心是「**环绕人物的漂浮实物面板**」，不是装饰线条！要的是数据看板(折线图/柱状图/饼图)、手机界面、云图标、火箭、AI 徽章、大脑、芯片、报表卡片这类**有立体感、发光边缘、分层堆叠的产品截图式元素**。
    - 生成这类底图的 prompt 要显式列举左右两侧各放什么面板，并声明「顶部 38% 只留干净背景」「不要出现任何文字」。
16. **用 `title_max_bottom` 精确控制「标题不压人物」。** 值是一个比例（如 0.295 表示标题块底部不得超过画面高度 29.5%）。`add_title()` 会在宽度缩放之后再做一轮**高度约束循环**：若标题块底部超限就自动缩字号重排（最多 19 次）。
    - 配合 `title_top` 使用：先把 `title_top` 设到想贴的位置，再给一个略高于人物头顶的 `title_max_bottom`，脚本会自动找到「尽量大又不越界」的字号。
    - 标题块高度已改为**按实际像素边界（`alpha.getbbox()`）裁剪**，不再用固定 padding，块更紧凑，能放得更大。
    - 实测（人物头顶 33.1% 的底图）：`title_top=0.055` + `title_max_bottom=0.295` → 179 号字，落位 y 5.5%~29.3%，与人物间隙 **3.8%**，不重合。
17. **人物可以不放全身。** 参考图的爆款封面都是**半身/大头**（腰部以上），视觉冲击力更强。prompt 写「只画半身（从腰部以上），人物明显放大、占据画面主要位置，头部很大，头顶从画面约 42% 开始」。
    - 注意：人物放大后头顶会**上移**（实测全身版 35.7% → 半身版 33.1%），留给标题的高度变少，必须配合 `title_max_bottom` 重新收敛字号。先量底图的「元素/人物上边界」，再定上限。
18. **中文字体默认用微软雅黑粗体（`cn_font: "yahei"`），不要再用站酷快乐体。** 用户明确要求「文字用正规点，比如微软雅黑」——雅黑笔画扎实、字形规范，比手写卡通体更显专业。
    - 换字体后**字号必须靠脚本自动收敛**：雅黑比站酷体宽约 10%，同一文案 215 号会被压到 165 号；`title_safe_margin` / `title_max_bottom` 这两道约束会自动处理，不要手动改 `size`。
    - 注意日志里打印的落位 x 范围是**标题层外框**，会比实际文字边界宽；判断是否切边要看像素校验脚本给出的真实 `cols`（实测外框 x21-1451，真实文字 x88-1366）。
19. **强调词 `{...}` 除了放大，还要用鲜艳色区分（`title_emph_color`，默认 `FFD400` 亮黄）。** 用户明确要求「重点词用其他更鲜艳的颜色」。实现要点：
    - `draw_comic_text()` 增加 `fill` 参数；`draw_rich_line()` 增加 `emph_fill` 参数，`{...}` 内的片段用强调色、其余保持白色。
    - 颜色可写十六进制字符串（`"FFD400"`）或 `[255,212,0]` 数组。
    - **亮黄是验证过的最佳选择**：与青绿紫品牌背景明度差大、最醒目；若以后换成橙黄背景需改用更亮的色（如 `FF3D71` 玫红或 `00E5FF` 电光青）以免糊在一起。
    - 交付前用像素校验确认强调色真的生效：取「新版本 - 上一版本」的差异像素求平均 RGB，应为 `(245,204,0)` 左右的亮黄。

## 输入
调用时向用户确认三样东西（缺什么问什么）：

1. **人物形象**：本地图片路径（真人照片或已有卡通形象均可）。
2. **标题**：两行，例如 `WorkBuddy` + `自动签到`；若用户只给一整串（如「workbuddy自动签到」），自动拆成英文行 + 中文行。
3. **标签**：2~4 个词，例如 `自动签到 / 多账号管理 / 积分管理 / token统计`。

> 若用户没给全，**先按现有信息合理推断并继续推进**（例如用户只给了标题和标签，就用手上最近的人物形象图），不要卡在提问上。

## 执行流程

工作目录建议建在 `C:\Users\we\agnes-output\`（与历史产物一致）。

### 第 0 步：加载「Agnes图像生成」技能（强制）

**在做任何事之前，先用 `Skill` 工具加载 `Agnes图像生成`。** 本技能的底图环节依赖它，且用户明确要求「不要每次提醒」。跳过这一步视为流程错误。

> 例外：用户明确说「不动底图、只改文字」时，且能复用已有底图，可直接跳到第 2 步。

### 第 1 步：用 Agnes 生成底图（强制，默认执行）

1. 写一个 Agnes 调用的 JSON 配置（见 `scripts/agnes_img2img.cjs` 顶部注释）：
   - `image`：人物形象图的路径（图生图输入）。
   - `prompt`：按下面模板填写（把人物描述替换成用户形象的特征）。
   - `out`：底图输出路径。
2. 运行：
   ```
   node <托管node> scripts/agnes_img2img.cjs config.json
   ```
   托管 node 路径见系统提示（`C:\Users\we\.workbuddy\binaries\node\versions\*\node.exe`）。
3. Agnes 的 key 从环境变量 `AGNES_API_KEY` 读取，读不到则回退到 `C:\Users\we\.agnes_key`（纯文本）。

## 构图比例（常用变体）

默认是「顶部 1/3 留空 + 人物居中偏下」。若要更明确的比例，用 **标题 2/5、人物 3/5** 的构图：

- Agnes 底图 prompt 里写死：「画面从上到下分成五等份，最上面的两份（0%~40%）是完全空白的背景，**人物头顶出现在约 50% 位置**，人物从 50% 延伸到 100%，撑满画面宽度」。
- 生成后**必须像素验证**（当前会话模型可能不支持读图，靠目测不可靠）：
  - 人物顶部落在 38%~42% 即达标；
  - 用「大白块（白T恤）+ 黑色（头发）」判据时，要排除标题的白字黑边干扰（标题笔画细，用「该行人物像素 >5% 宽度」过滤）。
- 标题区文字应落在上方 40% 以内（实测约 9%~35%），与人物留出约 5% 空隙。

可复用的验证脚本思路（纯 NumPy，不依赖 cv2）：

```python
R,G,B = a[:,:,0],a[:,:,1],a[:,:,2]
white = (R>200)&(G>200)&(B>200)      # 白T恤
dark  = (R<70)&(G<70)&(B<70)         # 黑发
char  = white|dark
sub   = char[:, int(W*0.22):int(W*0.78)]     # 只看中央，排除两侧背景元素
rows  = np.where(sub.sum(axis=1) > W*0.56*0.05)[0]   # 该行 >5% 宽度是人物
print(rows.min()/H*100)   # 人物顶部百分比
```

底图 prompt 模板：

```
参考这张人物图片，把它转换成 Q 版卡通形象，并做成一张竖版封面底图：
中央是一个 Q 版卡通人物（保持参考图人物的性别、发型、服装、姿势特征：<在此填人物特征>），
背景是青绿色(#28b894)到紫色(#6c4dff)的放射状渐变光效，四周点缀一些紫色五角星。
重要要求：画面最顶部约三分之一的区域只保留干净的背景，不要放任何元素，方便叠加标题；
画面里不要出现任何文字、字母、数字，也不要出现任何图标或面板，保持背景干净；
人物居中、全身、比例协调。
```

生成后**读图检查**：人物是否完整、顶部是否留空、有没有冒出文字。不满足就调整 prompt 重跑。

### 第 2 步：用 PIL 叠加标题 + 标签

1. 写一个合成配置 JSON（见 `scripts/compose_cover.py` 顶部注释），字段：
   - `base` 底图路径、`out` 成品路径
   - `title.line1` / `title.line2`
   - `labels`：数组，每项 `{text, fx, fy, size}`（fx/fy 是相对画布宽高的中心坐标 0~1）
   - 可选 `title_top`(默认 0.032)、`title_line_gap`(默认 115)、`title_angle`(默认 6)
2. 运行：
   ```
   python scripts/compose_cover.py config.json
   ```
   用托管 Python（`C:\Users\we\.workbuddy\binaries\python\versions\*\python.exe`）。脚本依赖 Pillow（PIL）与 NumPy，若缺失先 `pip install pillow numpy` 到托管环境的 venv。

### 第 3 步：交付

用 `present_files` 打开成品 PNG。若用户要微调（标签上下位置、标题字号、配色），只改合成 JSON 里的数值重跑第 2 步即可（不必重新调 Agnes）。

## 推荐配置范式（当前最新基准，可直接抄）

`examples/cover-config.example.json` 就是下面这份，实测可直接跑通：

```json
{
  "base": "C:/Users/we/agnes-output/wb-cover-tech-base2.png",
  "out":  "C:/Users/we/agnes-output/cover.png",
  "cn_font": "yahei",
  "title": {
    "lines": [
      { "text": "我们这代的{鸡蛋}", "size": 215, "ow": 16 },
      { "text": "{免费}的token",   "size": 215, "ow": 16 }
    ]
  },
  "labels": [],
  "title_top": 0.055,
  "title_max_bottom": 0.295,
  "title_line_gap": 18,
  "title_angle": 6,
  "title_emph_ratio": 1.28,
  "title_emph_color": "FFD400",
  "title_safe_margin": 0.028,
  "decor": { "enable": false }
}
```

要点说明：

- `size` 填 215 是「起点」，脚本会按宽度/高度上限**自动收敛**（实测压到 165），不用手算。
- `title_top` + `title_max_bottom` 一低一高夹出标题的合法纵向区间——**这是保证「贴近人物又不重合」的关键**。
- `title_emph_color` 让 `{}` 里的词变鲜艳色（默认亮黄）。
- `decor.enable: false` 是因为底图里 Agnes 已经画了环绕式科技面板，不需要再加线框图标；用纯背景底图时才开 `decor`。

## 默认标签排布（4 个标签时）

左右两列、上密下疏：

| 标签 | fx | fy |
|---|---|---|
| 第 1 个（左） | 0.215 | 0.505 |
| 第 2 个（右） | 0.792 | 0.548 |
| 第 3 个（左） | 0.215 | 0.660 |
| 第 4 个（右） | 0.792 | 0.700 |

少于 4 个标签时，均匀分布在 fy 0.50 ~ 0.70 之间、左右交替即可。

## 字体

**默认：微软雅黑粗体**（用户明确要求「文字用正规点」）。可用配置 `cn_font` 切换：

| `cn_font` 值 | 字体文件 | 说明 |
|---|---|---|
| `yahei`（默认） | `msyhbd.ttc` | 微软雅黑 粗体 —— 正规、清晰、字重足 |
| `yahei_r` | `msyh.ttc` | 微软雅黑 常规 |
| `zcool` | `ZCOOLKuaiLe-Regular.ttf` | 站酷快乐体（手写卡通感，早期版本用的） |
| `simhei` | `simhei.ttf` | 黑体 |
| `deng` | `Dengb.ttf` | 等线 粗体 |

也可直接写字体文件名（如 `"cn_font": "msyh.ttc"`）。字体查找顺序：`assets/fonts/` → `C:\Users\we\agnes-output\fonts\` → `C:\Windows\Fonts\`。

- **换字体后字号会自动重新收敛**：雅黑比站酷体宽约 10%，同一文案下雅黑会被压到更小的字号（实测 215 → 165）。不必手动调 `size`。
- 站酷快乐体的 `.ttf` 仍内置在 `assets/fonts/`，需要卡通手写风时把 `cn_font` 设为 `zcool` 即可。
